### 1.9 기본적인 서식 맞추기

``` C++
#include <iostream>
using namespace std;

int add(int x, int y)
{
    return x + y; // indenting(빈칸 만들기)
}

int main()
{
    int my_v = 1;
    int x = 4;
    int num_apples = 123;
    
    // this is important
    int x = 1 + 2; 

    // this is important too.
    int y = 3 + 4;

    // 하드 코딩
    // 변수 값을 리터럴 같이 고정된 값으로 직접 대입해주는 방식
}
}
```